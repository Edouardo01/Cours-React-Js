class MycoolComponent {
  static defaultProps = {
    ln10: true,
    requiered: false,
  }

  render() {
    const { ln10, name, requiered, value } = this.props

    return
  }
}

// definir une valeur par defaut
